{#
/**
 * @file
 *
 * Default product template.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - product: The rendered product fields.
 *   Use 'product' to print them all, or print a subset such as
 *   'product.title'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ product|without('title') }}
 *   @endcode
 * - product_entity: The product entity.
 * - product_url: The product URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes.addClass("commerce-pruduct-teaser") }}>
  <div class="card hoverable">

    {% if (product.variation_field_image) %}
        <div class="card-image">
          {{- product.variation_field_image[0] -}}
        </div>
    {% endif %}
    <div class="card-content">
      <span class="card-title"><a href="{{ product_url }}">{{- product.title -}}</a></span>
    </div>
    {% if (product.variation_price) %}
        <div class="card-action">
          {{- product.variation_price -}}
        </div>
    {% endif %}
  </div>
</article>
